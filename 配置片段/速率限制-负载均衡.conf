# 负载均衡站点速率限制配置
# 适用于多后端服务器的高可用架构
# 引入通用规则后，只需配置主 location

# 引入通用规则（包含API、后台、健康检查等location）
include /etc/nginx/配置片段/速率限制-通用规则.conf;

# 主 location - 标准限制
location / {
    limit_req zone=通用限制 burst=30 nodelay;
    limit_conn 连接限制 15;
}
