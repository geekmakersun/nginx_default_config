# PHP 应用站点速率限制配置
# 适用于 WordPress、Laravel、ThinkPHP 等
# 引入通用规则后，只需配置主 location

# 引入通用规则（包含登录、搜索、上传等location）
include /etc/nginx/配置片段/速率限制-通用规则.conf;

# 主 location - 标准限制
location / {
    limit_req zone=通用限制 burst=20 nodelay;
    limit_conn 连接限制 10;
}
