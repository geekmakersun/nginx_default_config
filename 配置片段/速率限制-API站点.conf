# API / 反向代理站点速率限制配置
# 适用于前后端分离、RESTful API 服务
# 引入通用规则后，只需配置主 location

# 引入通用规则（包含API、WebSocket、上传等location）
include /etc/nginx/配置片段/速率限制-通用规则.conf;

# 主 location - 较宽松（前端资源）
location / {
    limit_req zone=通用限制 burst=30 nodelay;
    limit_conn 连接限制 15;
}
